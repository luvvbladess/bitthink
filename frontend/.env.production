VITE_API_URL=/api
# VITE_WS_URL intentionally unset: ChatPage.tsx derives a same-origin ws(s)://
# URL from window.location so it goes through nginx's /chat/ws proxy instead
# of a baked-in public IP over plaintext.
